# Карта Marylee Content

Оновлено 10.09.2026. Цей файл і `AGENTS.md` — короткий старт для наступних змін, без повторного обходу всього проєкту.

## Де працює

- GitHub: https://github.com/cyfrovahata-bit/marylee-content — `main`.
- Застосунок: https://tiktok-chanel-production-84a1.up.railway.app/
- Railway: **YouTubeStories**, environment `production`, service `tiktok-chanel` (назва сервісу залишилася старою).
- projectId: `cc2719c7-ff14-42d5-ad78-6ba6e30aa84a`
- environmentId: `f7f78caf-8c15-4511-aa61-e6e6cf89e37b`
- serviceId: `1bf8f7e8-cac9-4d8c-9b90-8d48a6dae660`
- Окремий Volume: `/data`. Node 24, FFmpeg, SQLite, простий JS без npm-залежностей. Dockerfile + railway.json визначають збірку. Оновлення main запускає автодеплой.
- Drive: лише `YouTube Storis → Marylee Content`; parent ID `1sIcjn1IYI4uuw6TsBwyiKUKN6eNQUpXJ`.
- Робочий TikTok проєкт — інший репозиторій/сервіс; його не чіпати.

## Який файл читати

| Зміна | Файли |
|---|---|
| Сценарії, різні ідеї, публічні дані товару | `src/content.js`, `src/ai.js` |
| Голос, API-моделі, резерви | `src/config.js`, `src/ai.js`, `.env.example` |
| Фото, кадрування деталі, Reel, обкладинка | `src/montage.js` |
| Кнопки, редактор, календар | `web/app.js`, `web/index.html`; оформлення — `web/style.css` |
| HTTP API, перевірка змін, черга з UI | `src/server.js` |
| Пакетна генерація, відновлення, підготовка ввечері | `src/worker.js` |
| Розклад, ротація товарів, підказки публікації | `src/planner.js` |
| Каталог, збереження файлів, SQLite, ліміти | `src/catalog.js`, `src/files.js`, `src/store.js` |
| Імпорт і збереження ZIP | `src/drive.js`, `src/export.js` |

## Як проходить день

Каталог → план із двома товарами → тексти партіями до трьох суміжних слотів одного товару → збереження текстів → ілюстрація ранкового Reel → озвучка/монтаж → готові файли → ручна публікація.

Розклад за Києвом: 09:00 порада; 09:30 її поширення; 12:00 опитування; 14:00 товар A у каруселі; 15:30 деталь A; 18:30 знайомство з B; 19:30 продажний Reel B; 20:00 його поширення. 21:30 — необов’язкова сторіз. Автопідготовка завтра — 22:30.

- Тема поради не передається продажним слотам. Опис Reel разом із короткими фактами до 250 символів, далі 15–20 ключових слів і п’ять малих хештегів. Сторіз — до 90 символів.
- Тексти: `MARYLEE_TEXT_MODEL`, стандартно GPT-4.1. Зображення: `MARYLEE_IMAGE_MODEL`, стандартно gpt-image-1.
- Голос порад: `MARYLEE_TTS_ENGINE=openai`, `MARYLEE_OPENAI_VOICE=marin`. Старі `TTS_ENGINE` і `TTS_OPENAI_VOICE` не визначають голос Marylee.
- Продажний Reel: чотири короткі написи, приблизно 15 секунд, тиха звукова доріжка. Тільки оригінали товару.
- Ранкова ілюстрація: два образи, один платний запит, використовується також у фоні опитування. Зберігається до TTS/монтажу; повтор після збою її не генерує заново. Власні матеріали можна вибрати в редакторі.
- Добові межі: 24 текстові запити, 2 ілюстрації, 10 000 символів голосу; типовий резерв $5. Не збільшувати заради перевірок.
- «Перегенерувати день» оновлює тексти й файли неопублікованих матеріалів: для стандартних восьми слотів потрібно три текстові запити. «Підготувати матеріали» домонтовує чернетки й використовує наявні тексти та кеш. Для дрібних виправлень спочатку редагувати вручну.
- Схема ШІ залишає неактуальні поля порожніми (зокрема imagePrompt товарів і pollQuestion звичайних дописів), щоб не витрачати токени на них. Запитання опитування обирається з коротких завершених варіантів.

## API і збереження

- `GET /api/state?date=YYYY-MM-DD` — каталог, план, завдання, резерви, ознаки налаштування без ключів.
- `POST /api/plans/:date/prepare` — `{mode:"render"|"text"|"all", itemId?:"evening"}`. Без itemId працює партіями для всього неопублікованого дня.
- `PUT /api/plans/:date/items/:id` — ручні правки.
- `POST /api/jobs/:id/retry` — продовження зі збереженими контрольними точками.
- `GET /api/plans/:date/export` — ZIP дня. `GET /media/:id` — результат; відео підтримує Range.
- Записи: `/data/marylee.sqlite`. Оригінали та результати: `/data/media`; кеш голосу: `/data/cache`; тимчасові файли: `/data/work`.
- Для API-змін потрібен `X-Marylee: 1`; збережено перевірку Origin. Входу через пароль немає за рішенням власника.
- Повна перегенерація не редагує вже опубліковані записи. Поширення неопублікованого Reel підхоплюють нові файли батьківського матеріалу.

## Мінімальна перевірка

- Сценарії/генерація: `node --test test/ai.test.js test/content.test.js`
- Сервер/збереження/налаштування: `node --test test/core.test.js test/startup.test.js`
- Монтаж: `node --test test/media.test.js` — справжні FFmpeg, JPEG, MP4, SRT, ZIP; перевіряє тишу в продажному відео, перший кадр і повторне використання ілюстрації після помилки голосу.
- Синтаксис JS: `node scripts/check.js`

Не запускати всі перевірки повторно без конкретної причини. Детальний журнал — `docs/VALIDATION.md`.

## Поточні реальні дані для перевірки

Збережений тест власника — **2026-09-10**, два лляні костюми: шорти+жилет (464, 1170 грн) та брючний (468, 1350 грн), по шість оригінальних фото. Новий календар зберігає вибрану дату в URL; стартова дата без параметра — завтра. Перед заміною завжди перевірити статуси: власник міг уже опублікувати матеріал.

Не брати дроп-ціни з описів. Не створювати новий день, коли потрібно перевірити вже збережений. Актуальний commit/deployment перевіряти адресно через GitHub/Railway; SHA навмисно не дублюється тут як вічна константа.
